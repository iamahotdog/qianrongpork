千容肉品訂購 Web App v41.8｜訂單編號即時顯示

本次重點：
1. 成功頁不再等待 Google Sheet 才產生訂單編號。
2. 按下送出時即產生正式訂單編號：
   QR + 14碼時間 + 6碼亂數
3. Apps Script 使用前端產生的同一個訂單編號，並在後端檢查是否重複。
4. 成功頁立即顯示真正訂單編號，不再出現：
   - 建立中…
   - 已送出
5. 背景確認只負責取得後端驗算後的：
   - 正式總額
   - 配送方式
   - LINE 完整訂單內容
6. 背景確認最多約 6 秒，不再累積等待十多秒。
7. 修正 V41.6 後端重要欄位錯誤：
   - 「總包數」寫商品總包數
   - 「運費」寫配送費
   - 不再讓運費覆蓋總包數
8. 保留後端驗價、防重複 requestId、收件人電話、同訂購人同步等功能。

部署順序：
A. Google Apps Script
   - 貼入 v41.8 程式碼
   - 管理部署作業 → 編輯目前正式部署 → 新版本 → 部署
   - 不要新增新的 deployment

B. GitHub Pages
   - 更新 index.html

注意：
如果先前尚未執行 V41.6 的 migrateSheetLayoutV416_()，
請先完成欄位遷移與確認，再部署 V41.8。
