千容肉品 v42.2｜零等待訂單完成頁

目的：
移除客人端「訂單確認中…」等待流程。

前端：
1. 按下送出後立即進成功頁。
2. 訂單編號立即顯示。
3. 「複製訂單內容」立即可用。
4. 「前往 LINE 傳送訂單」立即可用。
5. 訂單文字恢復定稿格式：
   【千容肉品訂單】
   訂單編號：QR...
   商品
   ・品名 ×數量
   應付總額：$...
   配送方式：...
   匯款後五碼：.....
6. 背景仍會嘗試讀取後端確認結果；若後端回傳，會自動把 LINE 內容更新為後端正式金額。
7. 客人不再需要等待 Google Sheet 才能操作。

後端：
1. 沿用 V42.1 CacheService、單欄查找、後端驗價、防重複 requestId。
2. BACKEND_VERSION 更新為 v42.2。
3. Google Sheet 永遠是正式訂單金額來源。
4. 即使前端被 F12 改價，後臺仍依 PRODUCT_CATALOG 正式價格寫入。

重要：
成功頁顯示的即時金額是前端畫面計算值，主要供正常客人傳 LINE；
實際收款/對帳請以 Google Sheet 後端驗算金額為準。

部署：
1. Apps Script：貼入 v42.2 → 管理部署作業 → 編輯既有正式部署 → 新版本 → 部署。
2. GitHub Pages：更新 index.html。
3. health check 應回傳 version: v42.2。
